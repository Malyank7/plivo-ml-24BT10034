The final model improves the provided baseline through optimization and regularization rather than major architectural changes. AdamW with weight decay produced better optimization behaviour than the original Adam optimizer. Cosine annealing allowed the learning rate to decrease smoothly during the fixed 2000-step budget. Gradient clipping stabilized updates throughout training. Weight tying reduced redundant parameters while improving parameter efficiency. Dropout and improved initialization reduced overfitting and stabilized convergence. The context window was increased from 128 to 192 tokens, allowing the model to learn longer-range dependencies. Batch size and learning rate were tuned to improve convergence speed. The tokenizer and evaluation pipeline were kept unchanged to preserve compatibility. The final configuration achieved a BPB score of 2.4181 while satisfying all assignment constraints.